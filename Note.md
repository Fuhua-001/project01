# บันทึกการอัปโหลดโค้ดขึ้น Git (เริ่มแรก)
*วันที่บันทึก: 23 กรกฎาคม 2026, เวลา 16:48:33 น.*

การบันทึกโค้ดลง Git (เรียกว่าการ Commit) มีประโยชน์มากเวลาเราเขียนโค้ดพลาดแล้วอยากย้อนกลับมาเวอร์ชันเดิม 
สเตปการทำงานใน Terminal:

### 1. เช็คสถานะก่อน
```bash
git status
```
- เอาไว้เช็คว่ามีไฟล์ไหนบ้างที่มีการเปลี่ยนแปลง หรือเพิ่งสร้างใหม่ (สีแดงคือยังไม่เตรียมบันทึก)

### 2. เตรียมไฟล์ทั้งหมดเพื่อบันทึก
```bash
git add .
```
- `.` หมายถึง "ทั้งหมด" เป็นการเตรียมไฟล์ทั้งหมดที่มีการแก้ไขเข้าสู่สถานะเตรียมบันทึก (Staging Area) ถ้าย้อนไปพิมพ์ `git status` ไฟล์จะเปลี่ยนเป็นสีเขียว

### 3. ถ่ายรูปบันทึกประวัติ (Commit)
```bash
git commit -m "First commit: สร้างระบบ Login และโครงสร้าง Dashboard"
```
- เป็นการบันทึกประวัติ โดย `-m` คือข้อความกำกับ (Message) เพื่อให้รู้ว่าในรอบนี้เราทำอะไรไปบ้าง

---

# บันทึกคำอธิบายการทำงานของระบบ Login & Register
*วันที่บันทึก: 23 กรกฎาคม 2026, เวลา 15:17:45 น.*

โค้ดระบบถูกออกแบบมาด้วยหลักการ **"แบ่งแยกส่วนประกอบ" (Component-Based)** เพื่อให้โค้ดอ่านง่ายและแก้ไขสะดวก โดยแบ่งออกเป็น 3 ไฟล์หลัก ดังนี้:

---

### 1. ไฟล์ `app/page.tsx` (ตัวควบคุมหลัก)
ไฟล์นี้คือ "เวที" ที่คอยคุมว่าจะให้ใครออกมาแสดง ระหว่างฟอร์มเข้าสู่ระบบ หรือ ฟอร์มสมัครสมาชิก

**ส่วนสำคัญในโค้ด:**
- `"use client";` : บรรทัดนี้สำคัญมากใน Next.js เป็นการบอกว่าไฟล์นี้ทำงานที่ฝั่งผู้ใช้งาน (Browser) ทำให้เราสามารถสร้างลูกเล่นที่โต้ตอบกับผู้ใช้ได้ เช่น การคลิกปุ่มแล้วหน้าเปลี่ยนทันทีโดยไม่ต้องโหลดเว็บใหม่
- `useState` : คือเครื่องมือสำหรับจดจำสถานะ (State) ว่าตอนนี้เรากำลังเปิดหน้าต่างไหนอยู่

```tsx
const [isLogin, setIsLogin] = useState(true);

const toggleForm = () => {
  setIsLogin((prev) => !prev);
};
```
- `isLogin` : เป็นตัวแปรเก็บค่า `true` (แปลว่าตอนนี้แสดงหน้า Login อยู่) หรือ `false` (แสดงหน้า Register)
- `toggleForm` : เป็นฟังก์ชันคำสั่ง "สลับหน้า" เมื่อถูกเรียกใช้ มันจะสลับค่าจาก `true` เป็น `false` (และกลับกัน)

```tsx
<div className={`transition-opacity duration-300 ${isLogin ? 'opacity-100 block' : 'opacity-0 hidden'}`}>
  <LoginForm onToggle={toggleForm} />
</div>
```
- โค้ดตรงนี้คือเงื่อนไขว่า ถ้า `isLogin` เป็นจริง ให้แสดง `<LoginForm />` ถ้าเป็นเท็จให้ซ่อนไว้ 
- เรามีการส่ง `onToggle={toggleForm}` ข้ามไปให้ LoginForm ด้วย เพื่อให้ปุ่มในหน้า LoginForm สามารถสั่งสลับหน้าเว็บจากตรงนี้ได้

---

### 2. ไฟล์ `app/auth/LoginForm.tsx` (ชิ้นส่วนหน้าเข้าสู่ระบบ)
ไฟล์นี้คือ "นักแสดงคนที่ 1" มีหน้าที่แค่แสดงฟอร์มสำหรับเข้าสู่ระบบเท่านั้น

**ส่วนสำคัญในโค้ด:**
```tsx
interface LoginFormProps {
  onToggle: () => void;
}
```
- ส่วนนี้คือ TypeScript ทำหน้าที่กำหนดสเปคว่า "คอมโพเนนต์นี้ต้องรับค่าคำสั่ง `onToggle` เข้ามานะ" 

```tsx
const handleSubmit = (e: React.FormEvent) => {
  e.preventDefault();
  alert("ระบบกำลังเข้าสู่ระบบ (จำลอง)");
};
```
- `handleSubmit` : เมื่อผู้ใช้กดปุ่ม "เข้าสู่ระบบ" ฟังก์ชันนี้จะทำงาน
- `e.preventDefault();` : เป็นคำสั่ง **"ห้ามโหลดหน้าเว็บใหม่"** (ปกติเวลากด Submit ฟอร์ม เว็บจะรีเฟรชตัวเอง) เราหยุดมันไว้เพื่อที่เราจะได้ดึงข้อมูลไปเช็คกับ Database ได้โดยที่หน้าเว็บไม่กระพริบ
- `alert(...)` : ตอนนี้เป็นแค่ระบบจำลอง เลยเด้งกล่องข้อความจำลองขึ้นมา (ในอนาคตนำโค้ดเชื่อมต่อฐานข้อมูลมาใส่ตรงนี้ได้)

```tsx
<button onClick={onToggle}>สมัครสมาชิก</button>
```
- ปุ่มด้านล่างสุด เมื่อกดแล้วมันจะไปเรียกใช้คำสั่ง `onToggle` ที่หน้าหลัก `page.tsx` ส่งมาให้ ทำให้การสลับหน้าเกิดขึ้น

---

### 3. ไฟล์ `app/auth/RegisterForm.tsx` (ชิ้นส่วนหน้าสมัครสมาชิก)
ไฟล์นี้คือ "นักแสดงคนที่ 2" การทำงานแทบจะเหมือน LoginForm 100% แต่ต่างกันแค่:
- มีช่องให้กรอก **"ชื่อ-นามสกุล"** เพิ่มขึ้นมา
- ปุ่ม Submit เปลี่ยนชื่อเป็น "สมัครสมาชิก"
- ปุ่มด้านล่างสุดเปลี่ยนเป็นคำว่า "มีบัญชีอยู่แล้วใช่หรือไม่? เข้าสู่ระบบ" เพื่อสลับกลับไปหน้า Login 

---

### 💡 ทำไมถึงต้องแยกเป็น 3 ไฟล์?
แทนที่จะเขียนโค้ดยาว 300-400 บรรทัดไว้ในไฟล์เดียว การแยกแบบนี้ทำให้:
1. **พังยากขึ้น:** หากต้องการแก้สีปุ่มหน้า Login ก็เปิดแก้แค่ไฟล์ `LoginForm.tsx` โอกาสไปเผลอลบโค้ดของหน้า Register ก็จะไม่มี
2. **เอาไปใช้ซ้ำได้ง่าย:** หากในอนาคตต้องการให้มีกล่อง Login เด้งขึ้นมาในหน้าแสดงสินค้า ก็สามารถเรียกใช้คำสั่ง `<LoginForm />` โค้ดกล่องนี้ก็จะไปปรากฏที่หน้านั้นทันทีโดยไม่ต้องเขียนฟอร์มใหม่ตั้งแต่ต้น

---

# บันทึกคำอธิบายระบบ Dashboard
*วันที่บันทึก: 23 กรกฎาคม 2026, เวลา 15:26:33 น.*

หลังจากผู้ใช้เข้าสู่ระบบแล้ว จะถูกพาเข้ามาที่หน้าระบบจัดการภายใน (Dashboard) ซึ่งออกแบบให้มีแถบเมนูด้านซ้ายและเนื้อหาอยู่ด้านขวา การทำงานถูกแบ่งเป็นโฟลเดอร์ต่างๆ ตามหลักการ **Next.js App Router** (Nested Routing) ดังนี้:

---

### 1. ไฟล์ `app/dashboard/layout.tsx` (ตัวควบคุมหน้าต่างหลักของแดชบอร์ด)
ไฟล์นี้คือ **กรอบหน้าต่างหลัก (Layout)** ที่จะครอบทุกหน้าในแดชบอร์ดเอาไว้ 

**ส่วนสำคัญในโค้ด:**
```tsx
import Link from "next/link";
```
- `Link` : เป็นเครื่องมือของ Next.js ที่ใช้สำหรับเปลี่ยนหน้าจอโดยไม่ต้องรีเฟรชเว็บ (ต่างจากการใช้แท็ก `<a>` แบบปกติ)

```tsx
<aside className="w-64 bg-slate-900 border-r border-white/10 flex flex-col">
  {/* ...โค้ดส่วนเมนู... */}
  <Link href="/dashboard/customers">👥 ฐานข้อมูลลูกค้า (Customers)</Link>
</aside>
```
- `<aside>` : สร้าง **แถบเมนูนำทาง (Sidebar)** ที่อยู่ฝั่งซ้ายมือ ทำให้ไม่ว่าจะกดไปที่เมนูไหน แถบเมนูซ้ายมือก็จะไม่หายไป 
- `href="/dashboard/customers"` : เมื่อกดแล้ว ระบบจะพาไปยังหน้าโฟลเดอร์ `customers` ทันที

```tsx
<main className="flex-1 p-8 overflow-y-auto">
  {children}
</main>
```
- `{children}` : ตำแหน่งนี้คือพื้นที่ตรงกลางของจอ เนื้อหาจากไฟล์ `page.tsx` ของโฟลเดอร์ต่างๆ จะถูกนำมาแสดงตรงจุดนี้ตามเมนูที่เราเลือก

---

### 2. โฟลเดอร์หน้าต่างๆ (หน้าเนื้อหา)
แต่ละฟีเจอร์ถูกแยกโฟลเดอร์ออกจากกัน โดยในแต่ละโฟลเดอร์จะมีไฟล์ `page.tsx` เป็นตัวแสดงผล

**ตัวอย่างโครงสร้างโค้ด (ใช้ร่วมกันในทุกหน้า):**
```tsx
export default function CustomersPage() {
  return (
    <div>
      <h1 className="text-3xl font-bold">จัดการข้อมูลลูกค้า (Customers)</h1>
      <button>+ เพิ่มลูกค้าใหม่</button>
      {/* ...ตารางข้อมูล... */}
    </div>
  );
}
```
- แต่ละหน้าจะทำหน้าที่แค่ส่งก้อนเนื้อหา (เช่น ตารางลูกค้า, ปุ่มเพิ่มข้อมูล) กลับไปให้ `layout.tsx` เอาไปโชว์ในช่อง `{children}` เท่านั้น
- **หน้าต่างๆ ที่สร้างไว้ ได้แก่:**
  - `app/dashboard/page.tsx` (ภาพรวม / แดชบอร์ด)
  - `app/dashboard/quotations/page.tsx` (ใบเสนอราคา)
  - `app/dashboard/customers/page.tsx` (ฐานข้อมูลลูกค้า)
  - `app/dashboard/users/page.tsx` (จัดการผู้ใช้งาน)
  - `app/dashboard/inventory/page.tsx` (คลังสินค้า)

---

### 💡 ทำไมถึงต้องแยกเป็นโฟลเดอร์แบบนี้?
การสร้างโครงสร้างแบบ `/dashboard/ชื่อหน้า/page.tsx` เป็นมาตรฐานของ Next.js (เรียกว่า File-system Based Routing)
- แต่ละฟีเจอร์ทำงานแยกกันชัดเจน (Separation of Concerns) ไม่ปะปนกัน
- หากระบบไหนพัง (เช่น หน้าใบเสนอราคา Error) หน้าคลังสินค้าและหน้าอื่นๆ ก็จะยังเปิดใช้งานได้ตามปกติ
- ช่วยให้การจัดระเบียบโค้ดสำหรับโปรเจคที่ขยายใหญ่ขึ้นทำได้ง่ายมาก

---

# บันทึกการเชื่อมต่อหน้า Login ไปยัง Dashboard
*วันที่บันทึก: 23 กรกฎาคม 2026, เวลา 15:34:09 น.*

เมื่อผู้ใช้กรอกข้อมูลเสร็จและกด "เข้าสู่ระบบ" ระบบจะต้องพาย้ายหน้าต่างจาก `/` ไปยังโฟลเดอร์ `/dashboard` เราทำสิ่งนี้ได้ผ่านสิ่งที่เรียกว่า **Router** ครับ

### การแก้ไขในไฟล์ `app/auth/LoginForm.tsx`
**ส่วนสำคัญที่ถูกเพิ่มเข้าไป:**
```tsx
import { useRouter } from "next/navigation";
```
- `useRouter` : เป็นเครื่องมือของ Next.js ที่ทำหน้าที่เหมือนพนักงานขับรถ คอยพาย้ายหน้าจอไปมาโดยไม่โหลดหน้าเว็บใหม่

```tsx
export default function LoginForm({ onToggle }: LoginFormProps) {
  const router = useRouter(); // เรียกพนักงานขับรถมารอไว้
  // ...
```
- เราสร้างตัวแปร `router` ขึ้นมาเพื่อเรียกใช้งานเครื่องมือนำทาง

```tsx
  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault(); 
    router.push('/dashboard'); 
  };
```
- `router.push('/dashboard')` : นี่คือคำสั่ง **พาย้ายหน้าเว็บ** ทันทีที่คำสั่งนี้ทำงาน ผู้ใช้จะถูกส่งไปที่หน้าเว็บ `/dashboard` ที่เราสร้างไว้ในโฟลเดอร์ทันทีครับ 
- *(หมายเหตุ: ในระบบจริงจะมีการเขียนโค้ดตรวจสอบรหัสผ่านว่าถูกต้องไหมก่อนที่จะใช้คำสั่ง push นี้ครับ)*
